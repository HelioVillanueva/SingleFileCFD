Exemplo de barra simples (condução unidimensional) com temperaturas fixadas
nas pontas.

Pasta "3" é solução do OpenFOAM através do solver "laplacianFoam". O caso está
preparado para simplesmente executar o solver do OpenFOAM.

Para executar o solver em python, basta copiar o arquivo para a pasta do caso
e simplesmente executar no terminal:

python basicDiffusion.py

O resultado será escrito na pasta "1".

O caso pode ser visto no ParaView simplesmente carregando o arquivo
"BarraSimples.foam" dentro do ParaView. Caso OpenFOAM instalado, o mesmo
procedimento pelo comando "paraFoam" tambem pode ser usado.
